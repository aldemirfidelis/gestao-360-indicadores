node_modules
.pnpm-store
dist
build
.next
out
coverage
.turbo
*.tsbuildinfo

# Env (NUNCA commitar credenciais!)
.env
.env.*
!.env.example
!.env.production.example
!.env.droplet.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / IDE local config
.vscode
.idea
.DS_Store
Thumbs.db
.claude
.cursor

# Tests
playwright-report
test-results

# Prisma local sqlite (caso uso futuro)
*.db
*.sqlite
*.sqlite-journal

# Dumps de introspeccao do BSC legado (contem amostras de dados reais)
apps/api/scripts/legacy-bsc/output/

# Backups logicos da Administracao do Banco (podem conter dados reais)
apps/api/storage/

# Dumps/backups do Postgres local da droplet (migracao Neon->local e backup diario)
db-backups/
*.dump

# Documentos internos com dados reais (LGPD - nunca commitar)
Gestao_premio/

# Documentos institucionais gerados (case comercial / catalogo de modulos) - nao versionar
docs/modulos/
docs/case-institucional/
scripts/gerar-case-institucional.py
